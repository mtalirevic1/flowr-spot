export type User = {
    id: number
    first_name: string
    last_name: string
}

export type UserRegistration = {
    first_name: string
    last_name: string
    email: string
    password: string
    date_of_birth: string
}